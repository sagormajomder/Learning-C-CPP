#include <bits/stdc++.h>
using namespace std;
int main()
{
    int length, width;
    cout << "Enter Rectangular Length and Width respectively: ";
    cin >> length >> width;
    cout << "Rectangular Area is: " << length * width << endl;
}
